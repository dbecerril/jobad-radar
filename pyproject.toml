[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "jobad-radar"
version = "0.1.0"
description = "LLM-powered job ad analysis and study planner"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
